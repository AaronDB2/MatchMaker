version: 2.1

orbs:
  windows: circleci/windows@2.2.0

jobs:
  test:
    description: Setup and run application tests
    executor:
      name: windows/default
    steps:
      - add_ssh_keys:
          fingerprints:
            - "93:2f:35:23:39:35:75:33:af:78:93:99:88:86:46:a7"
      - checkout
      - run:
          name: "Print Working Directory"
          command: pwd
      - run:
          name: "Check files"
          command: cd ..
      - run:
          name: "Check files 2"
          command: pwd
      # - run:
      #     name: "Install project dependencies"
      #     command: dotnet.exe restore
      # - run:
      #     name: "Run Application Tests"
      #     command: dotnet.exe test
      # - run:
      #     name: "Print Working Directory"
      #     command: pwd
workflows:
  test:
    jobs:
      - test
